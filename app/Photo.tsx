import React from "react";
import Card from "@/components/Surfaces/Card";
import { Photo } from "@prisma/client";

const Photo = ({ name, user, caption, url }: Photo) => {
  return <Card>Photos</Card>;
};

export default Photo;
